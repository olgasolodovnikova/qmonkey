[project]
name = 'qmonkey'
version = '0.1.0'
description = 'Test package'
dependencies = [
    'numpy',
]

[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'

[tool.setuptools.packages.find]
where = ['src']
